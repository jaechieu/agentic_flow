import { MarkerType } from "reactflow";

export const defaultEdgeOptions = {
  type: "custom",
  markerEnd: {
    type: MarkerType.ArrowClosed,
    width: 20,
    height: 20,
    color: "#888",
  },
}; 